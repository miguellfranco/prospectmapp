{
  "version": 2,
  "installCommand": "npm install --legacy-peer-deps",
  "buildCommand": "npx prisma generate && (npx prisma db push --accept-data-loss || true) && npm run build",
  "framework": "nextjs",
  "regions": ["iad1"]
}
